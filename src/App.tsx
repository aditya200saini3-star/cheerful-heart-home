import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { BrowserRouter, Route, Routes } from "react-router-dom";
import { Toaster as Sonner } from "@/components/ui/sonner";
import { Toaster } from "@/components/ui/toaster";
import { TooltipProvider } from "@/components/ui/tooltip";
import Index from "./pages/Index.tsx";
import NotFound from "./pages/NotFound.tsx";
import DifficultySelector from "./pages/DifficultySelector.tsx";
import CategorySelector from "./pages/CategorySelector.tsx";
import Quiz from "./pages/Quiz.tsx";
import Results from "./pages/Results.tsx";

const queryClient = new QueryClient();

const App = () => (
  <QueryClientProvider client={queryClient}>
    <TooltipProvider>
      <Toaster />
      <Sonner />
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<Index />} />
          <Route path="/difficulty" element={<DifficultySelector />} />
          <Route path="/categories/:difficulty" element={<CategorySelector />} />
          <Route path="/quiz/:difficulty/:category" element={<Quiz />} />
          <Route path="/results/:difficulty/:category" element={<Results />} />
          {/* ADD ALL CUSTOM ROUTES ABOVE THE CATCH-ALL "*" ROUTE */}
          <Route path="*" element={<NotFound />} />
        </Routes>
      </BrowserRouter>
    </TooltipProvider>
  </QueryClientProvider>
);

export default App;
